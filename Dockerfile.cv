# Dockerfile for fake_cv service
FROM python:3.11-slim

WORKDIR /app

COPY fake_cv/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY fake_cv/ ./fake_cv

EXPOSE 9001

CMD ["uvicorn", "fake_cv.main:app", "--host", "0.0.0.0", "--port", "9001"]
